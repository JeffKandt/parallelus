# Branch Notebooks

Each active feature branch stores its notebooks under:

- `docs/branches/<slug>/PLAN.md`
- `docs/branches/<slug>/PROGRESS.md`

Files are generated by `make bootstrap slug=<slug>` and are folded/removed
during merge or archive workflows.
